{
    "name": "check_user",
    "description": "Check if a user exists using their telegram name.",
    "input_schema": {
        "type": "object",
        "properties": {
            "telegramName": {
                "type": "string",
                "description": "User's telegram name"
            }
        },
        "required": ["telegramName"]
    }
}