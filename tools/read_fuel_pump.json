{
  "name": "read_fuel_pump",
  "description": "Extract the fuel volume in liters and total cost from a fuel pump image, along with additional information like currency type, and provide a confidence score",
  "input_schema": {
    "type": "object",
    "properties": {
      "image_data": {
        "type": "string",
        "description": "Base64 encoded image data"
      }
    },
    "required": ["image_data"]
  }
}